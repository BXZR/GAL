using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CGModeBigPicture : MonoBehaviour {

	//CG模式的大图功能，超级简单，按下任意键自己关闭自己
	void Update () {
		if (Input.anyKeyDown)
			this.gameObject.SetActive (false);
	}
}
