using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//使用接口的方法来进行多态
//取消使用onStart这种非人为控制的config控制方法
//其实就是在已有实现方法上加一层调用视图
public interface configUser
{
	//保存到config
	//参数可以没有
	void saveToConfig ();
	//加载config
	//参数可以没有
	void  loadFromConfig ();


}